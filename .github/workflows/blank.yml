name: Run simple todocode

on:
  workflow_dispatch:

jobs:
  run-todocode:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run simple todocode (Python)
        run: python3 "simple todocode"
